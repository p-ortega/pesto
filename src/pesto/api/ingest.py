"""The estimate, the stream and the cancel (Plan 05-07): SSE progress over a background ingest."""

from __future__ import annotations

from fastapi import APIRouter

router = APIRouter(prefix="/api/run/ingest")
