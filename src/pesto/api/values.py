"""The field and the statistic (Plan 05-06): a realization or a permuted summary, same shape."""

from __future__ import annotations

from fastapi import APIRouter

router = APIRouter(prefix="/api/run/grid")
